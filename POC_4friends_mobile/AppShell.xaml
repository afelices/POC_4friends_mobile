<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="POC_4friends_mobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:POC_4friends_mobile"
    Shell.FlyoutBehavior="Flyout"
    Title="POC_4friends_mobile"
    FlyoutIcon="appicon.svg">

    <ShellContent
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <!--<Shell.FlyoutHeader>
        <Grid HeightRequest="100" BackgroundColor="DarkSlateBlue">
            <Image Source="moon.png"/>
        </Grid>
    </Shell.FlyoutHeader>

    <FlyoutItem Title="Astronomy" Icon="moon.png">
        <ShellContent Title="Moon Phase" Icon="moon.png"
            ContentTemplate="{DataTemplate local:MoonPhasePage}"/>

        <ShellContent Title="Sunrise" Icon="sun.png"
            ContentTemplate="{DataTemplate local:SunrisePage}"/>
    </FlyoutItem>

    <FlyoutItem Title="About" Icon="question.png">
        <ShellContent
            ContentTemplate="{DataTemplate local:AboutPage}"/>
    </FlyoutItem>-->

    <FlyoutItem Title="Credentials">
        <ShellContent Title="Credentials" Icon="appicon.svg" ContentTemplate="{DataTemplate local:Credentials}"/>
    </FlyoutItem>
    
</Shell>
